/* =====================================================================
 * lvgl_ui.h - 展示域（LVGL）：800×480 屏幕实时显示 ［框架线程］
 * ---------------------------------------------------------------------
 * 需求 FR-U01~U07：
 *   - 显示滤波/量程换算后的物理量（来自共享快照 EnvProc）
 *   - 告警显示、节点在线状态
 *
 * 数据来源：UI 以共享快照 node_snapshot_get 为主入口（决策 D9），
 *           不占用邮箱为主入口；邮箱参数保留供未来接收控制类消息。
 * UI 线程与其它域解耦：UI 崩溃不影响采集/处理/上报（FR-R03）。
 * ===================================================================== */

#ifndef LVGL_UI_H
#define LVGL_UI_H

#include <stdint.h>
#include "mbox.h"

/* UI 线程入口：配合 mbox_register(m, "ui", lvgl_ui_thread, m) 使用。
 * 内容留空（LVGL 初始化 / framebuffer / 刷屏逻辑后续填充）。 */
void *lvgl_ui_thread(void *arg);

#endif /* LVGL_UI_H */